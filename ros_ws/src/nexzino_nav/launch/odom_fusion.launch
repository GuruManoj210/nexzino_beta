<launch>
  <!-- Keep local odometry smooth and current for move_base. The EKF integrates
       the wheel encoder velocities into /odom and is the sole publisher of
       odom -> base_footprint. RTAB-Map consumes this /odom separately and owns
       the global map -> odom correction; delayed or temporarily lost visual
       tracking is therefore never injected back into the local odometry used
       by the controller. -->

  <node pkg="robot_localization" type="ekf_localization_node" name="ekf_odom_node" output="screen">
    <param name="frequency" value="30" />
    <param name="sensor_timeout" value="0.2" />
    <param name="two_d_mode" value="true" />
    <param name="publish_tf" value="true" />
    <param name="map_frame" value="map" />
    <param name="odom_frame" value="odom" />
    <param name="base_link_frame" value="base_footprint" />
    <param name="world_frame" value="odom" />

    <param name="odom0" value="/wheel_odom" />
    <param name="odom0_differential" value="false" />
    <param name="odom0_relative" value="false" />
    <param name="odom0_queue_size" value="2" />
    <param name="odom0_nodelay" value="true" />
    <!-- x,y,z, roll,pitch,yaw, vx,vy,vz, vroll,vpitch,vyaw, ax,ay,az -->
    <rosparam param="odom0_config">[false, false, false,
                                     false, false, false,
                                     true,  true,  false,
                                     false, false, true,
                                     false, false, false]</rosparam>

    <remap from="odometry/filtered" to="/odom" />
  </node>
</launch>
